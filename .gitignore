# Staging area for newly-received scans. Once a subject is merged, its
# source PDF is copied into the subject folder and tracked there.
New PYQ/

# Third-party / reference material, not authored here (~650 MB combined).
Old Sorted PYQ/
Prajit Notes/
Data Mining/Notes/
BEI IV-I(1).pdf

# LaTeX build artefacts
*.aux
*.log
*.toc
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.gz

# Loose scratch images in the project root; images/ folders are tracked.
/*.png
